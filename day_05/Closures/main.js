//Dans la conclusion de l'article d'introduction aux closures, l'auteur décrit la closure createCounter(). Réimplémentez cette closure.
//Dans cette partie de l'article une fonction createAdder() est décrite. Créez une closure createAdder(offset) qui prend en paramètre un nombre offset et qui retourne une fonction adder(b). La fonction adder(b) prend en paramètre un nombre b et retourne le nombre offset + b.
//Créez une closure createMultiplier(factor) suivant la même démarche que la closure createAdder(offset).
